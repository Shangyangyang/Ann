<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ainannan.tool.fileSort.mapper.FilePdfReadlineMapper">
	<sql id="FilePdfReadline_column_List" >
        a.id as "id",
		a.pid as "pid",
		a.readtime as "readtime",
		a.pagenum as "pagenum",
		a.p as "p",
		a.create_date as "createDate",
		a.create_user as "createUser",
		a.update_date as "updateDate",
		a.update_user as "updateUser",
		a.del_flag as "delFlag"
    </sql>

	<sql id="FilePdfReadline_left_join_include">
	</sql>

	<sql id="FilePdfReadline_where_List">
		a.del_flag = #{DEL_FLAG_NORMAL}
		<if test="readtime != null">
			and a.readtime = #{readtime}
		</if>
		<if test="pid != null and pid != ''">
			and a.pid = #{pid}
		</if>
	</sql>

	<select id="get" parameterType="String" resultType="FilePdfReadline">
		select
		<include refid="FilePdfReadline_column_List" />
		from file_pdf_readline a
		<include refid="FilePdfReadline_left_join_include" />
		where a.id = #{id}
	</select>

	<select id="findList" resultType="FilePdfReadline">
		select
		<include refid="FilePdfReadline_column_List" />
		from file_pdf_readline a
		<include refid="FilePdfReadline_left_join_include" />
		where <include refid="FilePdfReadline_where_List" />
		order by a.readtime asc
	</select>

	<insert id="insert">
		INSERT INTO file_pdf_readline(
			id,
			pid,
			readtime,
			pagenum,
			p,
			create_date,
			create_by,
			update_date,
			update_by,
			del_flag
		) values (
			#{id},
			#{pid},
			#{readtime},
			#{pagenum},
			#{p},
			#{createDate},
			#{createUser},
			#{updateDate},
			#{updateUser},
			#{delFlag}
		)
	</insert>

	<insert id="insertByList" parameterType="java.util.List">
		INSERT INTO file_pdf_readline(
		id,
		path,
		name,
		old_name,
		md5,
		suffix,
		size,
		create_by,
		create_date,
		del_flag,
		type
		)
		<foreach collection="list" item="entity"
				 separator=" union all ">
			SELECT
			#{entity.id},
			#{entity.path},
			#{entity.name},
			#{entity.oldName},
			#{entity.md5},
			#{entity.suffix},
			#{entity.size},
			#{entity.createBy},
			#{entity.createDate},
			#{entity.delFlag},
			#{entity.type}

		</foreach>
	</insert>

	<delete id="delete">
		update file_pdf_readline set del_flag = '1' where id = #{id}
	</delete>
</mapper>